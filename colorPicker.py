import random

print("rgb color picker")

import random
r = random.randint(0,255)
g = random.randint(0,255)
b = random.randint(0,255)
rgb = [r,g,b]


input("ask a question: ")
print(rgb)

print("to get the color, copy the numbers given in an online color picker!")
